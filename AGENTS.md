Use `docs/prd.md` as the source of truth for product behavior.

Project rules:
- Keep MVP scope small
- Do not add DB, payments, auth, or maps unless explicitly requested
- Store content in JSON files
- Save player progress in localStorage
- Prefer simple readable TypeScript
- Separate content data from UI logic
- Before large changes, propose file structure first